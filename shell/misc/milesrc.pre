# Use vi keybindings
export MILESRC_VI_BINDINGS="true"

# Start tmux if ssh connection
if [[ -n $(installed tmux) ]] &&
   [[ ! -f $HOME/.notmux ]] &&
   [[ -z $TMUX ]] &&
   [[ -n $SSH_CLIENT ]]
then
    exec tmux -2 new -A -s main
fi
